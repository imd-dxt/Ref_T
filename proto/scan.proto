syntax = "proto3";

package scan;

service ScanService {
  rpc ScanUrl (ScanRequest) returns (ScanResponse);
}

message ScanRequest {
  string url = 1;
}

message ScanResponse {
  string status = 1;
  string report = 2;
}